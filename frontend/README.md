# Frontend

This is the Expo React Native frontend application.

The main app code is located in the root `app/` directory.

## Structure

- `app/` - Expo Router app pages
- `assets/` - Images and static assets
- `hooks/` - Custom React hooks

## Running the Frontend

```bash
npm run dev
```
